/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ["imgs.search.brave.com"], // Define allowed domains for images
      domains: ["media.gettyimages.com"], 
    },
  };
  
  export default nextConfig;
  
